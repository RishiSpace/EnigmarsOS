---
# Configure mkinitcpio hooks for an installed EnigmarsOS system (not the live medium).
# initcpiocfg rewrites /etc/mkinitcpio.conf HOOKS based on detected root filesystem / encryption.

# Always include these at the start
prepend:
  - base
  - udev
  - autodetect
  - microcode
  - modconf
  - kms
  - keyboard
  - keymap
  - consolefont

# Always include these at the end
append:
  - block
  - filesystems
  - fsck

# Optional blocks inserted when applicable (Calamares fills these)
# encrypt / lvm2 / btrfs / resume are handled by the module when detected.
# Arch calamares still adds crc32c-intel for Intel+btrfs; fix-mkinitcpio.sh
# strips that name because kernels ≥6.14 no longer ship the module.
