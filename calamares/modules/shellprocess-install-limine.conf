---
# Install Limine bootloader on the target (Calamares has no native Limine backend).
dontChroot: false
timeout: 300
script:
  - command: "/usr/share/enigmaos/scripts/install-limine.sh"
    timeout: 300
i18n:
  name: "Install Limine bootloader"
