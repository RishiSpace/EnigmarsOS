---
# Run mkinitcpio for the installed EnigmarsOS kernel (not the live archiso preset).
# Requires shellprocess@fix-mkinitcpio to have rewritten presets first.
kernel: linux-enigmarsos
be_unsafe: false
