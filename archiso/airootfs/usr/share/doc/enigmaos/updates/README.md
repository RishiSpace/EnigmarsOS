# Updates

EnigmaOS is a rolling system based on Arch repositories.

```bash
sudo pacman -Syu
```

Or use **Welcome → Update System** / Plasma Discover.

Tips:

- Read `pacman` news / Arch news for manual interventions that also apply upstream.
- Prefer regular small updates over multi-month jumps.
- Flatpak apps: `flatpak update`
