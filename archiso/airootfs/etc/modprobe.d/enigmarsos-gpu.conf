# Live ISO: do not autoload NVIDIA on AMD/Intel. The live service
# still `modprobe nvidia` when an NVIDIA GPU is present (blacklist
# only blocks automatic loads).
blacklist nvidia
blacklist nvidia_drm
blacklist nvidia_modeset
blacklist nvidia_uvm
blacklist nouveau
