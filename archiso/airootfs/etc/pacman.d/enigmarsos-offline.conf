# Rolling linux-enigmarsos packages baked into the squashfs so Calamares
# can install them with no network. Prefer this over GitHub Latest.
[enigmarsos-offline]
SigLevel = Optional TrustAll
Server = file:///usr/share/enigmarsos/offline-repo
